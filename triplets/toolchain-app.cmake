if(DEFINED ENV{VCPKG_APP_C_COMPILER})
    set(CMAKE_C_COMPILER "$ENV{VCPKG_APP_C_COMPILER}")
endif()
if(DEFINED ENV{VCPKG_APP_CXX_COMPILER})
    set(CMAKE_CXX_COMPILER "$ENV{VCPKG_APP_CXX_COMPILER}")
endif()
